# Duet Backend Dependencies

# HTTP server
starlette>=0.40.0
uvicorn>=0.30.0

# MCP SDK with FastMCP
mcp>=1.2.0

# YAML frontmatter parsing
PyYAML>=6.0

# File watching (async-native, Rust notify-rs)
watchfiles>=1.0.0

# TOML parsing for the system_prompt deploy (stdlib `tomllib` only from Python 3.11)
tomli>=2.0; python_version < "3.11"
