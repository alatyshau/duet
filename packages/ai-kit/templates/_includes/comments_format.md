{# Секция 4: ФОРМАТ КОММЕНТАРИЕВ #}
{# Как читать и не затирать комментарии пользователя #}

## Формат комментариев

Пользователь может оставлять комментарии прямо в файлах. Уметь их распознавать и не затирать.

### Синтаксис

```markdown
- ::: АВТОР ::: текст комментария
  - вложенная строка (часть того же комментария)
    - ::: ДРУГОЙ ::: ответ на конкретную строку
```

### Правила распознавания

1. **Маркер** — `::: АВТОР :::` (например `::: АЛ :::`, `::: ИИ :::`)
2. **Всегда в списке** — комментарий начинается с `- ::: АВТОР :::`
3. **Вложенность** — всё что глубже маркера = часть комментария (пока не встретится новый маркер)

### Правила редактирования

1. **Не затирать** — при редактировании файла сохранять все комментарии
2. **Можно отвечать** — добавлять свой комментарий на том же уровне или к конкретной строке
3. **Prettify** — Secretary преобразует `::: АЛ :::` в `**АЛ | DATE**`

### Пример

```markdown
- текст документа
  - ::: АЛ ::: почему здесь два окна?
    - ::: ИИ ::: когнитивная нагрузка — три контекста сложнее удерживать
```

