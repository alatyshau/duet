В этом файле я покажу **живой сценарий** работы по предложенной методике. Мы пройдем цикл от сырой идеи до коммита и сохранения знаний.

**Легенда:**

* **Проект:** API на Python (FastAPI).
* **Задача:** Добавить ограничение частоты запросов (Rate Limiting).
* **Инструмент:** Chat в IDE (Cursor/VS Code) или Web-интерфейс (ChatGPT/Claude), куда мы копипастим файлы.

---

## Этап 1: Вход в контекст и режим «Clarify»

**Действие пользователя:**
Я открываю новый чат.

1. Прикладываю файлы: `.ai/context.md`, `.ai/active_task.md`.
2. Прикладываю промпт режима: `.ai/modes/01_clarify.md`.
3. Пишу запрос.

> **User:**
> Привет. Мы хотим добавить Rate Limiting для нашего API. Прочитай контекст, активируй режим **CLARIFY**. Не пиши код. Задай вопросы.

> **AI (Роль: Business Analyst):**
> Принято. Я изучил контекст. Чтобы обновить `active_task.md`, мне нужно прояснить требования к Rate Limiting:
> 1. **Гранулярность:** Мы ограничиваем по IP-адресу или по `user_id` (для авторизованных)?
> 2. **Хранилище:** В `context.md` указано, что мы используем PostgreSQL. Для лимитов мы будем использовать Redis (нужно добавить в стек) или in-memory (плохо для масштабирования)?
> 3. **Стратегия:** Жесткий лимит (Fixed Window) или скользящее окно (Sliding Window/Leaky Bucket)?
> 4. **Scope:** Это для всех эндпоинтов или только для тяжелых (например, генерация отчетов)?
> 
> 

> **User:**
> 1. По user_id.
> 2. Давай добавим Redis, давно пора.
> 3. Fixed Window подойдет.
> 4. Глобально для всего API.
> 
> 

> **AI (Роль: Business Analyst):**
> Понял. Фиксирую изменения.
> **Обновление `active_task.md`:**
> ```markdown
> ## Current Goal
> Реализовать Global Rate Limiting (Fixed Window) на базе Redis с привязкой к user_id.
> 
> ```
> 
> 

> ## Context for this task
> 
> 
> * Требуется поднять Redis в docker-compose.
> * Новая зависимость: redis-py (или aioredis).
> 
> 
> ```
> 
> Можем переходить к проектированию?
> 
> ```
> 
> 

---

## Этап 2: Режим «Architect»

**Действие пользователя:**
Я вижу, что мы поняли друг друга. Переключаю режим.

1. Прикладываю: `.ai/modes/02_architect.md`.

> **User:**
> Отлично. Переключись в режим **ARCHITECT**. Составь план.

> **AI (Роль: Architect):**
> Анализирую задачу. Учитывая `context.md` (FastAPI), предлагаю следующее решение:
> 1. **Infrastructure:** Добавить сервис `redis` в `docker-compose.yml`.
> 2. **Dependencies:** Добавить `redis` в `requirements.txt`.
> 3. **Config:** Обновить `config.py` для чтения `REDIS_URL`.
> 4. **Middleware:** Создать `src/middleware/rate_limiter.py`. Использовать Dependency Injection для подключения к Redis.
> 
> 
> **Предлагаемый план для `active_task.md`:**
> * [ ] Шаг 1: Инфраструктура (Docker + Requirements + Config).
> * [ ] Шаг 2: Реализация класса RedisClient (обертка).
> * [ ] Шаг 3: Реализация Middleware логики.
> * [ ] Шаг 4: Подключение к `main.py` и тесты.
> 
> 
> Утверждаем?

> **User:**
> Да, утверждаем. Запиши план в файл задачи.

---

## Этап 3: Режим «Execute»

**Действие пользователя:**
Теперь мы идем по шагам. AI запрещено самовольничать, он просто пишет код по плану.

1. Прикладываю: `.ai/modes/03_execute.md`.

> **User:**
> Переходим в режим **EXECUTE**. Выполняй **Шаг 1**.

> **AI (Роль: Senior Dev):**
> Выполняю Шаг 1: Инфраструктура.
> **Файл: `docker-compose.yml**`
> ```yaml
>   redis:
>     image: redis:alpine
>     ports:
>       - "6379:6379"
> 
> ```
> 
> 
> **Файл: `requirements.txt**`
> ```text
> redis>=5.0.0
> 
> ```
> 
> 
> **Файл: `src/config.py**`
> ```python
> class Settings(BaseSettings):
>     # ... существующие настройки
>     REDIS_URL: str = "redis://localhost:6379/0"
> 
> ```
> 
> 
> Шаг 1 готов. Могу переходить к Шагу 2?

> **User:**
> (Проверяю код, запускаю, всё ок).
> Да, давай сразу Шаг 2 и 3 вместе.

> **AI (Роль: Senior Dev):**
> *Генерирует код для RedisClient и Middleware...*

---

## Этап 4: Режим «Synthesize» (Самый важный!)

**Действие пользователя:**
Задача решена. Код написан. Обычно здесь мы просто закрываем чат, и контекст теряется. Но не сейчас.

1. Прикладываю: `.ai/modes/04_synthesize.md`.

> **User:**
> Всё работает. Тесты прошли. Активируй режим **SYNTHESIZE**. Подведи итоги и подготовь память к следующей задаче.

> **AI (Роль: Keeper):**
> Анализирую сессию...
> **1. Обновление `decisions.md` (Важно!)**
> Мы приняли архитектурное решение ввести Redis. Это нужно зафиксировать, чтобы в будущем не спорить "зачем нам Redis".
> ```markdown
> ## [2023-10-27] 005. Внедрение Redis
> **Decision:** Использовать Redis для кеширования и Rate Limiting.
> **Reasoning:** In-memory решения не работают при масштабировании на несколько реплик приложения.
> 
> ```
> 
> 
> **2. Обновление `context.md**`
> Нужно добавить Redis в раздел Tech Stack.
> **3. Очистка `active_task.md**`
> Задача выполнена. Я очищаю список "Progress Log" и "Current Goal".
> **Готов обновить файлы?**

> **User:**
> Да, обновляй.

---

## Итог

Что мы получили в конце?

1. **Чистый код** (благодаря режиму Architect и Execute).
2. **Обновленную документацию** (благодаря Synthesize).
3. **Историю решений** (ADR).

Если завтра я приду к другому AI (или другому чату) и скажу: *"Хочу сделать кеширование ответов"*, я просто скину ему обновленный `context.md` и `decisions.md`. AI увидит: *"Ага, у них УЖЕ есть Redis (решение 005), я буду использовать его"*.

**Это и есть R1: Эволюционируемость.**

---
