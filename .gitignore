# .gitignore
#
# ЧТО: Список файлов/папок, которые Git игнорирует.
#
# ЗАЧЕМ: Не коммитить производные файлы (node_modules, dist, кэши),
# которые восстанавливаются командами сборки или выносятся в ~/.duet-local/
#
# КТО ИСПОЛЬЗУЕТ: Git при каждой операции (status, add, commit)

# =============================================================================
# Node.js
# =============================================================================
# node_modules как папка И как файл (Google Drive создаёт файл из симлинка)
node_modules/
node_modules
npm-debug.log
yarn-error.log

# =============================================================================
# Build outputs
# =============================================================================
dist/
build/
out/
.turbo/

# =============================================================================
# Python
# =============================================================================
__pycache__/
*.pyc
.venv/
venv/

# =============================================================================
# Lean4 / Mathlib
# =============================================================================
.lake/
lake-packages/

# =============================================================================
# IDE / OS
# =============================================================================
.DS_Store
.idea/
*.swp
*.swo

# =============================================================================
# Caches / Logs
# =============================================================================
.eslintcache
*.log

# =============================================================================
# Claude Code
# =============================================================================
# settings.local.json — персональные настройки (permissions, model preferences).
# Не коммитим: у разных людей могут быть разные предпочтения по безопасности.
# НО: файл хранится на Google Drive (не в .duetignore) для синхронизации между машинами.
# Формат файла описан в .claude/settings.local.json.md
.claude/settings.local.json
